# Decisions — axnative-factoryplan-insight-agent

> 되돌리기 어렵거나, 나중에 "왜 이렇게 했지"가 나올 선택만 기록한다.
> 형식: **결정 / 근거 / 대안과 기각 사유**.
>
> 근거는 네 요소로 적는다 — 문서명 · 버전 · 날짜 · 위치(절 번호, 줄 번호, 파일 경로).
> 데이터 분석이 근거인 경우 재현 가능한 수치를 함께 남긴다.

---

## D-001. `DATASET_DIR` 기본값을 저장소 루트로 옮긴다

- **결정**: `insight_agent/config.py:7`의 기본값을 `~/Desktop/dataset_2`에서
  `PROJECT_ROOT / "dataset_2"`로 바꾸고, `.gitignore`에 `dataset_2/`를 추가한다.
- **근거**: 데이터가 저장소 안에 놓였다. `AGENTS.md:83`이 "`dataset_2`는 이 repo에
  절대 포함하지 않는다"고 규정하므로, 경로를 옮기는 것과 git에서 제외하는 것을
  함께 해야 규칙이 유지된다.
- **대안**: 사용자가 매번 `DATASET_DIR` 환경변수를 지정 → 기각. 실습생이 첫 실행에서
  걸려 넘어지는 지점을 하나 늘린다.

## D-002. xlsx를 CSV로 변환한다 (코드를 바꾸지 않는다)

- **결정**: `dataset_2`의 테이블별 `.xlsx` 6개를 같은 폴더에 `.csv`로 변환한다.
  `domain.load_tables()`의 `pd.read_csv`는 그대로 둔다.
- **근거**: 주어진 `dataset_2`에는 `.xlsx`만 있고 `.csv`가 없어 로딩이 실패했다.
  변환 서식은 `tests/fixtures/*.csv`(이 프로젝트의 정답 형식)를 기준으로 맞췄다.
  특히 `year_month`는 `YYYY-MM`이어야 한다 — `domain.py:142`가
  `str(production_date)[:7]`로 KPI 테이블과 조인하므로, `YYYY-MM-DD`로 쓰면
  영원히 매칭되지 않아 `dx_kpi_context`가 항상 null이 된다.
- **대안**: `load_tables`를 확장자 자동 탐지로 확장 → 기각(사람 판정). 가드레일의
  원래 의미인 "CSV 소스 vs 번들 xlsx 교차검증"이 "xlsx vs xlsx"로 바뀐다.
- **부작용**: 원본 xlsx가 갱신되면 재변환이 필요하다.

## D-003. CSV/xlsx 정합성 가드레일의 행 계산을 고친다 (완화가 아니다)

- **결정**: `domain.py`의 `check_source_consistency()`에서 `iter_rows`가 값 유무와
  무관하게 행을 세던 것을, 값이 하나라도 있는 행만 세도록 바꾼다.
- **근거**: 번들 xlsx의 작은 시트들이 데이터 아래에 서식만 남은 빈 행을 1000행까지
  선언하고 있어, `read_only` 모드의 `iter_rows`가 데이터 행이 아니라 **행 슬롯**을
  셌다. 5행짜리 차원 시트가 999행으로 보고되어 소스가 멀쩡한데도 매 실행 오탐했다.
  `fact_wafer_lot_yield`(1500)·`fact_fdc_chamber_sensor`(2500)만 통과한 것은 실데이터가
  1000행을 넘어 우연히 맞은 것이다.
- **`AGENTS.md:80`("가드레일을 조용히 완화하지 않는다")과의 관계**: 완화가 아니라
  계산 오류 수정이다. 불일치를 잡는 능력은 그대로다 — 기존 negative case 테스트
  3개가 계속 통과한다.
- **회귀 테스트**: `tests/test_guardrails.py`의
  `test_source_consistency_ignores_trailing_blank_rows`. 기존 테스트들이 이 버그를
  놓친 이유는 openpyxl로 시트를 깔끔하게 생성해 빈 서식 행이 없었기 때문이다.
  수정 전 로직으로 되돌리면 이 테스트가 실패하는 것을 확인했다.

---

# 데이터 함정 판정 (2026-08-28)

`dataset_2`에는 실습용으로 의도적으로 심어진 함정이 있다. 아래는 그 판정과 근거다.
**데이터는 기준선이므로 고치지 않는다.** 고치는 쪽은 시스템의 해석과 표현이다.

분석 재현: `insight_agent.domain.load_tables()`로 읽어 pandas로 계산. 기준 데이터는
로트 1,500행 · FDC 2,500행 · KPI 36개월(2023-09 ~ 2026-08).

## D-004. 함정 1 — "설비 이상이 수율 하락의 원인"은 데이터가 지지하지 않는다. UNKNOWN으로 답한다

- **결정**: 인과를 추정해 채우지 않는다. 근거가 없으면 "모르겠다"로 답한다.
- **근거**:
  - `corr(die_yield_pct, 로트당 인터록 건수) = +0.0050` — 사실상 0.
  - 결함 유형별 평균 `die_yield_pct`: None(Clean) 88.16 / Bridge Defect 88.20 /
    Gate Particle 88.18 / Pattern Collapse 88.39 / Under-etching 88.12.
    **무결함 로트와 결함 로트의 수율이 같다.**
  - 인터록 보유 로트 88.25 vs 미보유 88.18 — 오히려 근소하게 높다.
  - 상위 `CLAUDE.md`의 근거 규율 2·3번: "못 대면 UNKNOWN으로 남긴다.
    추정으로 채우지 않는다. 그럴듯한 추측이 가장 비싼 오류다."
- **기각된 반대 가설**: "인터록이 보호 조치로 작동해 수율을 지켰다"(개입의 역설).
  멈춤이 있었다면 폐기·재작업에 흔적이 남아야 하는데 없다 —
  `scrap_wafer_qty` 평균 1.046(정상) vs 1.062(인터록), `prime_wafer_qty` 22.378 vs 22.383.
- **주의**: `die_yield_pct`에는 강한 시간 추세가 있다(2023-Q3 약 84% → 2026-Q3 약 91%).
  추세를 통제하지 않으면 아무 변수나 수율과 상관있어 보인다.

## D-005. 함정 2 — 에이전트 5개 중 운영에 쓰이는 것은 2개다. 결측이 아니라 차원 과잉설계로 읽는다

- **결정**: `AGT-VM-01` · `AGT-RTM-04` · `AGT-YDA-05`에 그래프 엣지를 만들어 채우지
  않는다. "5개 중 실제로 필요한 것은 2개"라는 사실 자체가 판정이다.
- **근거**: `fact_fdc_chamber_sensor.controlling_ai_agent` 참조 횟수 —
  `AGT-APC-03` 2,366건 / `AGT-FDC-02` 134건 / 나머지 셋 **0건**.
  `graph/builder.py:104`가 이 컬럼에서만 `controlled_by` 엣지를 만들므로,
  세 에이전트는 노드로만 존재하고 관계가 없다.
- **영향**: README가 예시로 든 GraphRAG 질문 "이 에이전트가 제어하는 다른 챔버는?"이
  5개 중 3개에 대해 빈 결과를 돌려준다. 이는 버그가 아니라 데이터가 말하는 사실이다.
- **대안**: `vm_pred_thickness_nm` 컬럼을 근거로 `AGT-VM-01` 엣지를 추가 → 기각(사람 판정).
  근거가 있긴 하지만, 차원 테이블의 과잉설계를 드러내는 것이 이 실습의 학습 목표다.

## D-006. 함정 3 — 인터록·VM오차·에이전트 배정은 하나의 물리 사건이다. 근거를 중복 계수하지 않는다

- **결정**: 통합 리포트가 이 셋을 독립된 근거처럼 나열하지 않는다.
  `fdc_anomaly_row_count`(합집합)를 기준 수치로 쓰고,
  `fdc_interlock_vm_coincident_count`(교집합)를 함께 실어 겹침을 드러낸다.
- **근거 — 세 컬럼은 하나의 사건에서 파생된다**:
  1. **물리적으로 실재하는 단일 고장 모드**다. 인터록 134건 중 **132건이 챔버 압력·
     플라즈마 임피던스·서셉터 온도 세 개 모두 +2σ 이상**(정상군 기준).
     압력 최소 +3.50σ(평균 +5.93σ), 임피던스 최소 +3.03σ(평균 +4.66σ),
     온도 평균 +4.37σ. RF 파워(+0.29σ)·공정 가스(−0.71σ)는 무관하다.
     라벨만 붙은 가짜가 아니다.
  2. **VM 오차는 그 사건의 결과다.** 부호가 결정적이다 — 정상군은 평균 −0.001%,
     음수 비율 49.2%로 대칭인데, 인터록군은 평균 **+2.650%, 음수 비율 0.0%**
     (범위 +1.913 ~ +3.615). 실측이 예측보다 **항상 두껍다**. 압력·온도가 오르면
     증착이 과다해지므로 물리적으로 일관된다.
  3. **에이전트 배정도 결과다.** `AGT-FDC-02`의 정의된 책임이
     "챔버 내 **플라즈마 임피던스**/RF 헌팅 사전 감지"(`dim_ai_automation_role`)이다.
     임피던스가 이상해진 행에 임피던스 담당 에이전트가 붙는 것은 당연하다.
     `(controlling_ai_agent == 'AGT-FDC-02') == (fdc_interlock_flag == 1)`이
     2,500행 전부에서 성립한다. **대응자를 원인으로 착각하는 함정이다.**
- **왜 위험한가**: 근거가 셋으로 보이면 "설비 이상이 수율을 떨어뜨렸다"는 결론에
  확신이 붙는다. 실제 상관은 +0.005다(D-004). **함정 3은 함정 1로 넘어가는 다리다.**
  상위 `CLAUDE.md`가 출처를 네 요소(문서·버전·날짜·**위치**)로 적으라고 요구하는
  이유가 이것이다 — 위치까지 적으면 같은 행을 세 번 세고 있다는 게 드러난다.
- **조치**: `domain.build_causal_report`에 합집합·교집합 필드를 추가하고,
  `agents/narrative.py`의 템플릿과 LLM 프롬프트가 이를 기준으로 서술하게 했다.
  에이전트 목록에는 "같은 행에서 파생 — 별도 근거 아님"을 명시한다.
  회귀 테스트는 `tests/test_narrative.py`와
  `tests/test_domain.py::test_build_causal_report_reports_overlap_between_interlock_and_vm`.
- **부수 사실**: `get_fdc_anomalies`의 `max_vm_error_pct`를 1.2 ~ 1.9 어디로 바꿔도
  결과가 134건으로 동일하다. 두 군 사이에 0.767%p(1.1463 ~ 1.9134)의 빈 구간이 있어
  그 범위의 임계치 튜닝은 이 데이터에서 아무 효과가 없다.

---

## D-007. 통합 리포트는 인과를 확정하지 않는다. 판정은 항상 UNKNOWN이고 사유를 함께 싣는다

- **결정**: `build_causal_report`가 `causal_verdict` 필드를 반드시 포함한다.
  `verdict`는 항상 `"UNKNOWN"`이고, `reason`과 `explanation`이 **왜** 근거가 없는지를
  구분해 밝힌다. 가드레일(`harness/guardrails.py`)이 이 필드의 존재와 값을 강제한다.
- **사유 네 갈래**:

  | reason | 상황 |
  |---|---|
  | `no_fdc_anomaly` | FDC 이상이 없어 인과를 따질 근거 자체가 없다 |
  | `no_baseline` | 비교할 동일 공정 노드 평균이 없다 |
  | `no_yield_deficit` | 이상은 있으나 수율 저하가 없다 |
  | `coincident_only` | 둘 다 있으나 비교군이 없어 동시 발생까지만 말할 수 있다 |

- **근거**:
  - **단일 로트에는 비교군이 없다.** 같은 조건에서 이상이 없었을 때의 수율을 모르므로,
    이상과 저수율이 함께 보여도 인과가 아니라 동시 발생이다.
  - **모집단에서도 근거가 없다.** D-004 참조 — 상관 +0.005.
  - 인터록 보유 로트 128건 중 **수율 저하가 있는 것은 52건(41%)뿐이고 76건(59%)은
    저하가 아예 없다.** 이상이 수율로 이어진다는 규칙성이 없다.
  - 상위 `CLAUDE.md` 근거 규율 2·3번.
- **가드레일이 `UNKNOWN`만 허용하는 이유**: 이 리포트의 구조로는 인과를 세울 수 없다.
  나중에 제대로 된 인과 분석(비교군 설계, 시간 추세 통제)을 붙인다면
  `ALLOWED_CAUSAL_VERDICTS`를 넓히는 것이 그 변경의 명시적인 일부여야 한다.
  여기서 걸리는 것이 **의도된 마찰**이다.
- **대안**: 수율 저하 + FDC 이상이 겹치면 "원인 추정"으로 표기 → 기각.
  그럴듯한 추측이 가장 비싼 오류라는 것이 이 프로젝트의 전제다. 76건이 반례인데
  52건만 보고 규칙을 세우면 확증 편향을 코드로 굳히는 것이 된다.
- **주의 — `coincident_only` 판정은 사실상 생산 시점의 대리 지표다**:
  비교 기준인 `product_node_avg_die_yield_pct`는 3개년 전체 평균이라 시간 추세
  (2023-Q3 약 84% → 2026-Q3 약 91%)를 통제하지 않는다. 그 결과 판정이 설비 상태가
  아니라 **생산 연도에 따라 갈린다.** 인터록 보유 로트 128건의 연도별 분포:

  | 생산 연도 | `coincident_only` | `no_yield_deficit` | 저하 판정 비율 |
  |---|---|---|---|
  | 2023 | 14 | 2 | **87.5%** |
  | 2024 | 30 | 11 | 73.2% |
  | 2025 | 6 | 35 | 14.6% |
  | 2026 | 2 | 28 | **6.7%** |

  `corr(생산일, 수율차) = +0.701`. 초기 로트는 거의 전부 "저하", 후기 로트는 거의
  전부 "저하 없음"으로 기울어진다. **`coincident_only`를 인과의 방증으로 읽으면
  안 된다** — 같은 설비 이상이라도 2023년이면 저하, 2026년이면 저하 없음이 된다.
- **알려진 부채**: 위 편향을 없애려면 비교 기준을 3개년 평균이 아니라
  **동일 공정 노드 × 동일 분기** 평균으로 바꿔야 한다. 지금은 하지 않았다 —
  이번 변경의 목표는 인과 확정을 막는 것이고, 기준선 교체는 판정 값 자체를 바꾸는
  별개의 결정이라 근거와 함께 따로 기록해야 한다.
- **회귀 테스트**: `tests/test_domain.py`의 `test_judge_causality_never_asserts_causation`
  (네 갈래 전부) · `test_judge_causality_treats_noise_sized_gaps_as_no_deficit`,
  `tests/test_guardrails.py`의 `test_validate_report_rejects_asserted_causation`
  (`CONFIRMED`를 넣으면 거부) · `test_validate_report_rejects_unknown_without_a_reason`
  (사유 없는 UNKNOWN은 판단 회피이므로 거부),
  `tests/test_narrative.py`의 `test_summary_never_asserts_a_cause`.
